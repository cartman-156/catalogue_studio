/**
 * ArrayEditor - Edits array-type fields with add, remove, reorder
 * (This is now implemented in ArrayField component)
 */

// Re-export from the field component
export { default } from './fields/ArrayField';
